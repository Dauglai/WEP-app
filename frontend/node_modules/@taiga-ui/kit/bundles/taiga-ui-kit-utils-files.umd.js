(function (global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports, require('@taiga-ui/cdk')) :
    typeof define === 'function' && define.amd ? define('@taiga-ui/kit/utils/files', ['exports', '@taiga-ui/cdk'], factory) :
    (global = typeof globalThis !== 'undefined' ? globalThis : global || self, factory((global["taiga-ui"] = global["taiga-ui"] || {}, global["taiga-ui"].kit = global["taiga-ui"].kit || {}, global["taiga-ui"].kit.utils = global["taiga-ui"].kit.utils || {}, global["taiga-ui"].kit.utils.files = {}), global.cdk));
})(this, (function (exports, cdk) { 'use strict';

    var BYTES_PER_KB = 1000;
    var BYTES_PER_MB = 1000 * BYTES_PER_KB;
    function tuiFormatSize(units, size) {
        if (size === undefined) {
            return null;
        }
        if (size < BYTES_PER_KB) {
            return size + " " + units[0];
        }
        if (size < BYTES_PER_MB) {
            return (size / BYTES_PER_KB).toFixed(0) + " " + units[1];
        }
        return cdk.tuiRound(size / BYTES_PER_MB, 2).toLocaleString("ru-RU") + " " + units[2];
    }

    function tuiGetAcceptArray(accept) {
        return accept
            .toLowerCase()
            .split(",")
            .map(function (format) { return format.trim(); });
    }

    /**
     * Generated bundle index. Do not edit.
     */

    exports.tuiFormatSize = tuiFormatSize;
    exports.tuiGetAcceptArray = tuiGetAcceptArray;

    Object.defineProperty(exports, '__esModule', { value: true });

}));
//# sourceMappingURL=taiga-ui-kit-utils-files.umd.js.map
