(function (global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports) :
    typeof define === 'function' && define.amd ? define('@taiga-ui/kit/utils/format', ['exports'], factory) :
    (global = typeof globalThis !== 'undefined' ? globalThis : global || self, factory((global["taiga-ui"] = global["taiga-ui"] || {}, global["taiga-ui"].kit = global["taiga-ui"].kit || {}, global["taiga-ui"].kit.utils = global["taiga-ui"].kit.utils || {}, global["taiga-ui"].kit.utils.format = {})));
})(this, (function (exports) { 'use strict';

    /**
     * Converts a string to an HSL color
     * @param value string to convert
     * @return HSL color string
     */
    // @bad TODO: convert stringHashToHsl to stringHashToRgb
    function tuiStringHashToHsl(value) {
        if (value === "") {
            return "";
        }
        var hash = 0;
        for (var i = 0; i < value.length; i++) {
            hash = value.charCodeAt(i) + ((hash << 5) - hash);
            hash &= hash;
        }
        var hue = hash % 360;
        var saturation = 60 + (hash % 5);
        var lightness = 80 + (hash % 5);
        return "hsl(" + hue + "," + saturation + "%," + lightness + "%)";
    }

    /**
     * Generated bundle index. Do not edit.
     */

    exports.tuiStringHashToHsl = tuiStringHashToHsl;

    Object.defineProperty(exports, '__esModule', { value: true });

}));
//# sourceMappingURL=taiga-ui-kit-utils-format.umd.js.map
