import { Options as PrettierOptions } from 'prettier';
interface Options {
    file: string;
    config: PrettierOptions;
}
export declare function tuiPostPrettierFormat({ file, config }: Options): void;
export {};
