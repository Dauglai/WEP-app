import { TemplateRef, ViewContainerRef } from '@angular/core';
import { TuiContextWithImplicit } from '@taiga-ui/cdk/interfaces';
import * as i0 from "@angular/core";
export declare class TuiRepeatTimesContext implements TuiContextWithImplicit<number> {
    readonly $implicit: number;
    constructor($implicit: number);
}
/**
 * Directive similar to ngFor but using a number of repetitions rather than an array
 *
 * {@link TuiRepeatTimesDirective.tuiRepeatTimesOf requested number of times}.
 * {@link TuiRepeatTimesContext context} for every instance of the template inherits outer context and stores
 * {@link TuiRepeatTimesContext.$implicit index} of a template instance.
 */
export declare class TuiRepeatTimesDirective {
    private readonly viewContainer;
    private readonly templateRef;
    set tuiRepeatTimesOf(count: number);
    constructor(viewContainer: ViewContainerRef, templateRef: TemplateRef<TuiRepeatTimesContext>);
    private addContainers;
    private removeContainers;
    static ɵfac: i0.ɵɵFactoryDeclaration<TuiRepeatTimesDirective, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<TuiRepeatTimesDirective, "[tuiRepeatTimes][tuiRepeatTimesOf]", never, { "tuiRepeatTimesOf": "tuiRepeatTimesOf"; }, {}, never>;
}
