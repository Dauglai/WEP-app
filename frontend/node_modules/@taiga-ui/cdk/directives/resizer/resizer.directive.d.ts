import { ElementRef, EventEmitter } from '@angular/core';
import * as i0 from "@angular/core";
export declare class TuiResizerDirective {
    private readonly resizeable;
    tuiResizer: readonly [x: number, y: number];
    readonly tuiSizeChange: EventEmitter<readonly [x: number, y: number]>;
    x: number;
    y: number;
    width: number;
    height: number;
    constructor(resizeable: ElementRef<HTMLElement>);
    get cursor(): string;
    onTouchStart({ touches }: TouchEvent): void;
    onMouseDown(x: number, y: number): void;
    onMouseMove({ x, y, buttons }: MouseEvent): void;
    onTouchMove({ touches }: TouchEvent): void;
    onMouseUp(): void;
    onMove(x: number, y: number): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<TuiResizerDirective, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<TuiResizerDirective, "[tuiResizer]", never, { "tuiResizer": "tuiResizer"; }, { "tuiSizeChange": "tuiSizeChange"; }, never>;
}
