import { InjectionToken } from '@angular/core';
import { Observable } from 'rxjs';
/**
 * Window height accounting for disappearing address bar
 */
export declare const TUI_WINDOW_HEIGHT: InjectionToken<Observable<number>>;
