import { ExistingProvider, FactoryProvider, Type } from '@angular/core';
import { TuiPoint } from '@taiga-ui/core/types';
export declare abstract class TuiPositionAccessor {
    abstract readonly type: string;
    abstract getPosition(rect: ClientRect): TuiPoint;
}
export declare function tuiPositionAccessorFor(type: string): FactoryProvider;
export declare function tuiAsPositionAccessor(useExisting: Type<TuiPositionAccessor>): ExistingProvider;
